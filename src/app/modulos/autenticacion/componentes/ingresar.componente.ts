import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { Router } from '@angular/router'; import { ApiServicio } from '../../../nucleo/servicios/api.servicio';
@Component({ standalone: true, imports: [FormsModule], template: `<main class="container py-5"><section class="card mx-auto p-4" style="max-width:420px"><h2 class="text-primary">Sistema Comercial</h2><p>Inicie sesión para continuar.</p><input class="form-control mb-2" [(ngModel)]="usuario" placeholder="Usuario"><input class="form-control mb-3" [(ngModel)]="clave" type="password" placeholder="Clave"><div class="text-danger mb-2">{{error}}</div><button class="btn btn-primary w-100" (click)="ingresar()">Ingresar</button></section></main>` }) export class IngresarComponente { private api = inject(ApiServicio); private router = inject(Router); usuario = 'administrador'; clave = 'CambiarEnSecrets'; error = ''; ingresar() { this.api.autenticar(this.usuario, this.clave).subscribe({ next: r => { localStorage.setItem('token', r.token); this.router.navigateByUrl('/compras'); }, error: () => this.error = 'Credenciales inválidas o API no disponible.' }); } }
