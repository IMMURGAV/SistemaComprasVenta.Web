import { Component,inject,OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { ApiServicio } from '../../../nucleo/servicios/api.servicio'; import { ResumenKardex,DetalleOperacion } from '../../../nucleo/entidades/modelos';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<main class="contenedor container py-4"><div class="card p-4"><h3>Registrar venta</h3><div class="row g-2"><div class="col-md-5"><select class="form-select" [(ngModel)]="idProducto" (change)="seleccionar()"><option [ngValue]="0">Seleccione producto</option><option *ngFor="let p of productos" [ngValue]="p.idProducto">{{p.nombreProducto}} — Stock: {{p.stockActual}}</option></select></div><div class="col-4 col-md-2"><input class="form-control" type="number" min="1" [(ngModel)]="cantidad" placeholder="Cantidad"></div><div class="col-4 col-md-2"><input readonly class="form-control" [value]="seleccion?.precioVenta||0"></div><div class="col-4 col-md-3"><button class="btn btn-primary w-100" (click)="agregar()">Agregar</button></div></div><small *ngIf="seleccion">Disponible: {{seleccion.stockActual}} | Precio venta: {{seleccion.precioVenta|currency:'PEN'}}</small><table class="table mt-3"><thead><tr><th>Producto</th><th>Cant.</th><th>Precio</th><th>Subtotal</th><th>IGV</th><th>Total</th></tr></thead><tbody><tr *ngFor="let d of detalles"><td>{{nombre(d.idProducto)}}</td><td>{{d.cantidad}}</td><td>{{d.precio|currency:'PEN'}}</td><td>{{d.cantidad*d.precio|currency:'PEN'}}</td><td>{{d.cantidad*d.precio*.18|currency:'PEN'}}</td><td>{{d.cantidad*d.precio*1.18|currency:'PEN'}}</td></tr></tbody></table><button class="btn btn-success" [disabled]="!detalles.length" (click)="guardar()">Guardar venta</button><div class="alert mt-3" [class.alert-danger]="error" [class.alert-info]="!error" *ngIf="mensaje">{{mensaje}}</div></div></main>`}) export class VentasComponente implements OnInit {api=inject(ApiServicio);productos:ResumenKardex[]=[];detalles:DetalleOperacion[]=[];idProducto=0;cantidad=1;seleccion?:ResumenKardex;mensaje='';error=false;ngOnInit(){this.api.resumenKardex().subscribe(x=>this.productos=x);}seleccionar(){this.seleccion=this.productos.find(x=>x.idProducto===this.idProducto);}nombre(id:number){return this.productos.find(x=>x.idProducto===id)?.nombreProducto??'';}agregar(){if(!this.seleccion)return;if(this.cantidad>this.seleccion.stockActual){this.error=true;this.mensaje='La cantidad no debe ser mayor al stock disponible.';return;}this.detalles.push({idProducto:this.idProducto,cantidad:this.cantidad,precio:this.seleccion.precioVenta});this.mensaje='';}guardar(){this.api.registrarVenta({detalles:this.detalles}).subscribe({next:()=>{this.error=false;this.mensaje='Venta y movimiento de salida registrados.';this.detalles=[];},error:e=>{this.error=true;this.mensaje=e.error||'No se pudo registrar la venta.';}});}}
