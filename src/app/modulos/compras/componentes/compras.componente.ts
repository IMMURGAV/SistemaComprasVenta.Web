import { Component, inject, OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { ApiServicio } from '../../../nucleo/servicios/api.servicio'; import { Producto, DetalleOperacion } from '../../../nucleo/entidades/modelos';
@Component({ standalone: true, imports: [CommonModule, FormsModule], template: `<main class="contenedor container py-4"><div class="card p-4"><h3>Registrar compra</h3><div class="row g-2"><div class="col-md-5"><select class="form-select" [(ngModel)]="idProducto"><option [ngValue]="0">Seleccione producto</option><option *ngFor="let p of productos" [ngValue]="p.idProducto">{{p.nombreProducto}} - {{p.nroLote}}</option></select></div><div class="col-4 col-md-2"><input class="form-control" type="number" min="1" [(ngModel)]="cantidad" placeholder="Cantidad"></div><div class="col-4 col-md-2"><input class="form-control" type="number" min="0" [(ngModel)]="precio" placeholder="Costo"></div><div class="col-4 col-md-3"><button class="btn btn-primary w-100" (click)="agregar()">Agregar</button></div></div><button class="btn btn-outline-secondary btn-sm mt-2" (click)="mostrarModal=true">+ Registrar producto</button><table class="table mt-3"><thead><tr><th>Producto</th><th>Cantidad</th><th>Costo</th><th></th></tr></thead><tbody><tr *ngFor="let d of detalles;let i=index"><td>{{nombre(d.idProducto)}}</td><td>{{d.cantidad}}</td><td>{{d.precio|currency:'PEN'}}</td><td><button class="btn btn-sm btn-danger" (click)="detalles.splice(i,1)">×</button></td></tr></tbody></table><button class="btn btn-success" [disabled]="!detalles.length" (click)="guardar()">Guardar compra</button><div class="alert alert-info mt-3" *ngIf="mensaje">{{mensaje}}</div></div><div class="modal d-block bg-dark bg-opacity-50" *ngIf="mostrarModal"><div class="modal-dialog"><div class="modal-content p-3"><h5>Registrar producto</h5><input class="form-control mb-2" [(ngModel)]="nuevo.nombreProducto" placeholder="Nombre"><input class="form-control mb-2" [(ngModel)]="nuevo.nroLote" placeholder="Nro. lote"><input class="form-control mb-2" [(ngModel)]="nuevo.costo" type="number" placeholder="Costo"><div class="small text-muted mb-2">Precio de venta sugerido: costo × 1.35 = {{nuevo.costo*1.35|currency:'PEN'}}</div><button class="btn btn-primary" (click)="crearProducto()">Guardar producto</button> <button class="btn btn-link" (click)="mostrarModal=false">Cancelar</button></div></div></div></main>` }) export class ComprasComponente implements OnInit { api = inject(ApiServicio); productos: Producto[] = []; detalles: DetalleOperacion[] = []; idProducto = 0; cantidad = 1; precio = 0; mensaje = ''; mostrarModal = false; nuevo: any = { nombreProducto: '', nroLote: '', costo: 0 }; ngOnInit() { this.cargar(); } cargar() { this.api.listarProductos().subscribe(x => this.productos = x); } nombre(id: number) { return this.productos.find(x => x.idProducto === id)?.nombreProducto ?? ''; } agregar() { if (this.idProducto && this.cantidad > 0 && this.precio > 0) this.detalles.push({ idProducto: this.idProducto, cantidad: this.cantidad, precio: this.precio }); } crearProducto() { this.nuevo.precioVenta = this.nuevo.costo * 1.35; this.api.registrarProducto(this.nuevo).subscribe(() => { this.mostrarModal = false; this.cargar(); }); } guardar() { this.api.registrarCompra({ detalles: this.detalles }).subscribe({ next: () => { this.mensaje = 'Compra y movimiento de entrada registrados.'; this.detalles = []; }, error: e => this.mensaje = e.error || 'No se pudo guardar.' }); } }
