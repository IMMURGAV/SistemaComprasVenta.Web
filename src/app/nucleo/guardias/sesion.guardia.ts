import { CanActivateFn,Router } from '@angular/router'; import { inject } from '@angular/core'; export const sesionGuardia:CanActivateFn=()=>localStorage.getItem('token')?true:inject(Router).createUrlTree(['/ingresar']);
